#Colors
set-option -sa terminal-overrides ",xterm*:Tc"

set -g pane-active-border-style "fg=blue"
#setw -g window-style 'fg=colour247,bg=colour236'

#set -g cursor-blink on

#Mouse
set -g mouse on

# Proper start of windows at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

#Window Novagition
bind -n M-H previous-window
bind -n M-L next-window

bind -n M-J select-pane -t :.-
bind -n M-K select-pane -t :.+
# plugins

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'
# set Colors in Statusline

set -g @plugin 'niksingh710/minimal-tmux-status'
#set -g @plugin 'ofirgall/tmux-window-name'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-restore 'on'
set -g @continuum-boot 'on'
set -g @continuum-systemd-start-cmd 'start-server'

#set vi mode
set-window-option -g mode-keys vi

#key bindings for copy mode
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

#key bindings for more panes
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# create a default sessions called home
if-shell "tmux has-session -t home 2>/dev/null" '' 'new-session -d -s home -c /home/stefan/'

run '~/.config/tmux/plugins/tpm/tpm'
